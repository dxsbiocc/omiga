#!/usr/bin/env Rscript
args <- commandArgs(trailingOnly = TRUE)
plugin_root <- "{{ template.pluginRoot }}"
script <- file.path(plugin_root, "scripts", "pca_matrix.R")
status <- system2("Rscript", c(script, args))
if (!identical(status, 0L)) quit(save = "no", status = status)
