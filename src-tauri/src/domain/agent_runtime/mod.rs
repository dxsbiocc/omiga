//! Rust-native agent runtime surface: routing + persistence hooks aligned with Claude Code semantics.
//! Implementation details stay in `chat_input_target`, `agents::background`, and `persistence`.

pub use crate::domain::chat_input_target::ChatInputTarget;
